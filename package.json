{
  "name": "dt0",
  "version": "0.0.0",
  "main": "index.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "clean": "tsc --build --clean",
    "build:example": "tsc --build example.tsconfig.json",
    "clean:example": "tsc --build --clean example.tsconfig.json",
    "dev": "tsc && jest",
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules jest"
  },
  "dependencies": {
    "binaryen": "^111.0.0",
    "retsac": "^0.8.0"
  },
  "devDependencies": {
    "@types/jest": "^28.1.5",
    "@types/node": "^18.0.4",
    "cross-env": "^7.0.3",
    "jest": "^29.4.3",
    "ts-jest": "^29.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}
