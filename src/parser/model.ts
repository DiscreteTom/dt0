export type CompilerOptions = {
  /** Enable this to print debug info. */
  debug?: boolean;
  /** Enable this to see if there is any error in the compiler. */
  checkAll?: boolean;
};

export type CompileOptions = { optimize?: boolean };
